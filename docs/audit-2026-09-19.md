# Earlier code audit: current disposition

The original dated report is preserved [here](history/pre-0.8.0-refresh/audit-2026-09-19.md). It describes an older source snapshot and must not be used as the current feature list.

The 0.8.0 code includes actor/resource payment receipts, per-card roll/application claims, queued status reconciliation and persistent selected-target application receipts. The current [flow map](flow-map.md) explains ownership and recovery; [verification](verification.md) distinguishes automated checks from live testing.

Still worth checking live: linked/unlinked token identity through native damage/undo after scene changes, multiplayer handoff, simultaneous operations and compatibility with other HUD/status/dice modules. Differing token/actor display names alone do not establish a wrong-target mutation bug.

This refresh did not independently reproduce every historical finding or implement new fixes. Current open status belongs in [the backlog](../BACKLOG.md); the archived report remains evidence of what was investigated at that time.
